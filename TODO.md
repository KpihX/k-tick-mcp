# TODO

- [ ] Add deployment smoke tests against the real homelab endpoint after the first GitLab rollout.
- [ ] Add an SSH operator guide for `deploy/` (compose lifecycle, health checks, logs, rollback).
- [ ] Add authenticated admin HTTP mutation endpoints if a second remote admin surface beyond Telegram becomes necessary.
- [ ] Extend the Telegram bridge with richer read-only commands (`/config`, `/jobs`, `/mcp`) once the first production rollout is stable.
