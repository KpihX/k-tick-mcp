# TODO

- [ ] Add Telegram admin bridge commands (`/status`, `/health`, `/logs`, `/restart`) backed by the homelab HTTP/admin surface.
- [ ] Add authenticated remote admin actions once the Telegram bot command contract is fixed.
- [ ] Add deployment smoke tests against the real homelab endpoint after the first GitLab rollout.
- [ ] Add an SSH operator guide for `deploy/` (compose lifecycle, health checks, logs, rollback).
